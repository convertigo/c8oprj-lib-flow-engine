version: 1
icon: "mdi:select-search"
description: "Reads a nested value from a JSON object."
props:
  source:
    label: "source object"
    kind: "expression"
    type: "object"
    default: "local.source"
    description: "Object or array expression used as the selector root."
  path:
    label: "data path"
    kind: "selector"
    type: "string"
    mode: "read"
    sourceProperty: "source"
    default: ""
    description: "Relative path to read inside the source object. Supports indexes like rss.channel.item[0].title."
  out:
    label: "out"
    kind: "path"
    mode: "write"
    default: "local.value"
    description: "Scope path receiving the selected value."
hooks:
  file: "json.select.hooks.js"
implementation:
  runtime: "rhino"
  file: "json.select.js"
