version: 1
icon: "mdi:select-search"
description: "Reads a nested value from a JSON object."
props:
  source:
    label: "source"
    kind: "expression"
    type: "object"
    default: "local.source"
    description: "Object expression used as source."
  path:
    label: "path"
    kind: "path"
    type: "string"
    mode: "read"
    sourceProperty: "source"
    default: ""
    description: "Object path to read inside source. Supports indexes like rss.channel.item[0].title."
  out:
    label: "out"
    kind: "path"
    mode: "write"
    default: "local.value"
    description: "Scope path receiving the selected value."
hooks:
  file: "json.select.hooks.js"
implementation:
  runtime: "rhino"
  file: "json.select.js"
